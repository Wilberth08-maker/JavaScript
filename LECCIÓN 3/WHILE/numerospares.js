index = 10; // Inicializar en el 10
while(index  >= 1){ // El While se detiene cuando el número sea menor que 1
    
    if(index % 2 === 0){ // Se divide entre dos el número y si el residuo es 0 significa que es par
        
        console.log(index);    
    }
    
    index--; // Se decrementa uno
}